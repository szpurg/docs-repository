<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>{% block title %}Via Medica Docs repository{% endblock %}</title>

        {# Linki do Bootstrap CDN #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.10.24/css/dataTables.bootstrap4.min.css"/>        
        
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
        
        {# Miejsce na dodatkowe style, które mogą być dodane w innych szablonach #}
        {% block stylesheets %}
            <link href="/css/main.css" rel="stylesheet">
        {% endblock %}
    </head>
    <body>
        {# Navbar lub inny układ można tu umieścić #}
        <nav class="navbar navbar-expand-lg navbar-light bg-light container">
            <div class="container-fluid vmHead">
                <img id="headerLogo" src="https://www.viamedica.pl/sites/default/files/logo.png" />
                <a class="navbar-brand" href="/">Via Medica Docs repository</a>
            </div>
        </nav>

        <div class="container mt-4 container-body">
            {# Blok na zawartość stron, które rozszerzają ten szablon #}
            {% block body0 %}
                {% block breadcrumbsBox %}
                    {% if breadcrumbs is defined and breadcrumbs|length %}
                        <div class="breadcrumbs">
                            {% for bc in breadcrumbs %}
                                <div class="breadcrumb">
                                    {% if bc.href %}
                                    <a href="{{ bc.href }}">{{ bc.label }}</a>
                                    {% else %}
                                        {{ bc.label }}
                                    {% endif %}
                                </div>
                            {% endfor %}
                        </div>
                    {% endif %}
                {% endblock %}
                {% block manageCategory %}{% endblock %}
                {% block pageTitle %}
                    {% if parentCategory is defined and parentCategory %}
                        <div class="row">
                            <div class="col-12">
                                <h1 class="pageTitle">{{ parentCategory.name }}</h1>
                            </div>
                        </div>
                    {% endif %}
                {% endblock %}
                {% block body %}
                {% endblock %}
            {% endblock %}
        </div>

        {# Footer #}
        <footer class="footer mt-auto py-3 bg-light">
            <div class="container">
                <span class="text-muted">&copy; 2024 Via Medica Docs repository</span>
            </div>
        </footer>

        {# Dodanie jQuery i Bootstrap JavaScript #}
        <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
        
        {# DataTable: #}
        <script src="https://cdn.datatables.net/1.10.24/js/jquery.dataTables.min.js"></script>
        <script src="https://cdn.datatables.net/1.10.24/js/dataTables.bootstrap4.min.js"></script>
        
        {# Miejsce na dodatkowe skrypty, które mogą być dodane w innych szablonach #}
        {% block javascripts %}
        {% endblock %}
    </body>
</html>